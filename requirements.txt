# LiteLLM proxy — pinned for reproducible image builds.
# Chosen: 1.99.0, the currently validated AI Alchemy gateway release.
# This is an intentional stability pin, not a claim that 1.99.0 is always the
# newest upstream release. Upgrade only through a reviewed build and semantic
# ChatGPT subscription/Hugging Face routing validation.
litellm[proxy]==1.99.0

# FastAPI — intentionally NOT pinned here.
# The 1.97.0 gateway pinned fastapi==0.140.1 to work around
# litellm/proxy/management_endpoints/management_v1/common.py doing
# `from fastapi.dependencies.utils import get_flat_dependant`, a symbol FastAPI
# removed in 0.141.0 (tracked as BerriAI/litellm#35763). LiteLLM 1.99.0 fixes
# this: management_v1/common.py now uses `get_flat_params()` for
# fastapi>=0.140.7, so `import litellm.proxy.proxy_server` and the management_v1
# routes import cleanly against the naturally-resolved fastapi (0.141.x at the
# time of this upgrade). Re-add an explicit pin only if a future released
# source and image smoke test proves a new incompatibility.

# Prometheus callback — required when
# litellm_settings.callbacks includes "prometheus". LiteLLM's [proxy] extra
# does not install the client library even though the callback imports it at
# startup. This is the version required by LiteLLM's official proxy docs.
prometheus-client==0.20.0

# OpenTelemetry — required when litellm_settings.callbacks: ["otel"] is set.
# LiteLLM imports from opentelemetry.sdk and the OTLP HTTP exporter; the
# `[proxy]` extra does not pull these in, so we add them explicitly.
# Pinned to a single 1.31.x stack so api / sdk / exporter stay aligned
# (mismatched versions across this trio are the usual breakage mode).
# 1.31.x is the lowest line whose importlib-metadata range is compatible
# with litellm's pin (>=8.5.0) — earlier 1.27/1.28 conflict.
opentelemetry-api==1.31.1
opentelemetry-sdk==1.31.1
opentelemetry-exporter-otlp-proto-http==1.31.1

# Prisma client — required when DATABASE_URL is set so LiteLLM can
# initialize Postgres for spend logs, virtual keys, user/team mgmt, and
# admin-UI auth. The `[proxy]` extra does NOT pull this. The Dockerfile
# additionally runs `prisma generate` against LiteLLM's schema.prisma to
# bake the engine binaries into the image.
prisma==0.11.0
